services:
  redis:
    image: redis:7-alpine
    ports:
      - "6380:6379"

  dev:
    build: .
    depends_on:
      - redis
    environment:
      REDIS_HOST: redis
      REDIS_PORT: "6379"
    volumes:
      - .:/app
    stdin_open: true
    tty: true
    entrypoint: /bin/bash
